:- encoding(utf8).
:- consult('schedule.pl').

% -----------------------------
% Базовые запросы
% -----------------------------

% Когда у группы g101 предмет math?+% ?- когда_у_группы(g101, math, Day, Slot, Room).

% Какие аудитории свободны в среду в 10:40 (slot 2)?+% ?- свободна_аудитория(wed, 2, Room).

% У каких групп занятия ведет преподаватель petrov?+% ?- группы_преподавателя(petrov, Groups).

% Сколько пар в неделю у группы g102?+% ?- нагрузка_группы(g102, Count).

% Какие дни свободны у преподавателя orlova?+% ?- свободные_дни_преподавателя(orlova, Days).

% Предметы группы g103:
% ?- group_subjects(g103, Subjects).

% Расписание преподавателя kuznetsova:
% ?- teacher_schedule(kuznetsova, Lessons).

% Занятость аудитории r201 в четверг:
% ?- room_schedule(r201, thu, Slots).

% Окна в расписании g101:
% ?- окна_в_расписании(g101, Windows).

% День с минимальной нагрузкой для g104:
% ?- день_с_минимальной_нагрузкой(g104, Day).

% -----------------------------
% Уровень 2: проверки и метрики
% -----------------------------

% Проверка конфликтов:
% ?- нет_конфликтов_групп, нет_конфликтов_преподавателей, нет_конфликтов_аудиторий.

% Перегруженные преподаватели (>20 часов в неделю):
% ?- перегруженные_преподаватели(Teachers).

% Можно ли поменять местами две пары:
% ?- L1 = занятие(g101, math, ivanova, mon, 1, r101),
%    L2 = занятие(g102, math, ivanova, tue, 1, r101),
%    можно_переставить(L1, L2).

% -----------------------------
% Уровень 3: генерация CLP(FD)
% -----------------------------

% Сгенерировать валидное расписание:
% ?- генерировать_расписание(Schedule), length(Schedule, Len).

% Экспорт в CSV:
% ?- генерировать_расписание(Schedule),
%    экспортировать_csv('generated_schedule.csv', Schedule).

% Экспорт в HTML:
% ?- генерировать_расписание(Schedule),
%    экспортировать_html('generated_schedule.html', Schedule).
